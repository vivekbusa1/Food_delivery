# Base URL for the backend REST API (must include version prefix).
# In local dev, prefer /api/v1 so Vite proxies to the backend (avoids CORS / port 5000 conflicts).
VITE_API_URL=/api/v1

# App name shown in the sidebar / browser title
VITE_APP_NAME=Delivery Admin Panel
