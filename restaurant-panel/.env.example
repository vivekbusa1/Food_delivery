# Base URL of the Delivery App backend REST API
# Prefer /api/v1 in local dev so Vite proxies to backend :5001
VITE_API_URL=/api/v1

# Optional: base URL used to resolve relative image paths returned by the API
VITE_ASSET_URL=http://127.0.0.1:5001

# App display name
VITE_APP_NAME=Restaurant Panel
